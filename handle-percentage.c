#include "main.h"
/**
 * handle_percentage - a function that prints %.
 * @format: a string
 * Return: Number of characters printed.
 */
int handle_percentage(const char *format)
{
	_putchar(*format);
	return (1);
}
